// Define the Counter interface
public interface Counter {

    // Method to count words in a given sentence
    int countWords(String sentence);

    // Method to count letters in a given sentence
    int countLetters(String sentence);

    // Method to get the length of the given sentence
    int getLength(String sentence);
}
